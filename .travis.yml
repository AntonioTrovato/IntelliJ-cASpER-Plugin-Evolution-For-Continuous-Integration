language: java

jdk:

  - oraclejdk14

script:

  - java -jar Copia/copia2/cASpER-master/Code/build/libs/casper-1.1.2.jar HELLOWORLD

deploy:
  provider: pages
  skip_cleanup: true
  github_token:
    secure: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: main